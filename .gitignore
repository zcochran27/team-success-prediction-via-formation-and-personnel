# Data — large / proprietary; do not commit (but keep the directories tracked)
data/raw/*
data/processed/*
data/team_seasons/*
!data/raw/.gitkeep
!data/processed/.gitkeep
!data/team_seasons/.gitkeep

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Jupyter
.ipynb_checkpoints/

# LaTeX build artifacts (regenerated on every compile; keep the .pdf)
*.aux
*.fdb_latexmk
*.fls
*.log
*.out
*.toc
*.bbl
*.blg
*.synctex.gz

# Virtual environments
.venv/
venv/
env/

# Editor / OS
.vscode/
.idea/
.claude/
.DS_Store
Thumbs.db

scripts/
test.ipynb

# Archived prior-iteration models, datasets, and artifacts. Preserved on
# disk for reference but not tracked. The repo now focuses on the
# per-half + substitution-aware pipeline (see PROJECT_DESCRIPTION.md).
legacy/